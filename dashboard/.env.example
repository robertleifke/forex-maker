# API Configuration
# Override only for local dev — production uses relative URLs automatically
NEXT_PUBLIC_API_URL=http://localhost:8000/api
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws

# Must match ENGINE_API_TOKEN in the engine .env
NEXT_PUBLIC_API_TOKEN=
